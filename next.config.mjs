/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If you want to use the React Compiler (experimental), use this instead:
  // experimental: {
  //   reactCompiler: true,
  // },
};

export default nextConfig;